# python-rest-counter-server
A simple python server that hold a temp value of a counter that you call and increment

# to configure your environment to run this server 
use configure.sh

# to run the server 
python app.py run
